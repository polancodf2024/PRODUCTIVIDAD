streamlit==1.32.2
pandas>=2.2.0  # Versión compatible con Python 3.13
numpy>=1.24.0  # Requerido por pandas

# Excel support
openpyxl>=3.1.0  # Para leer/escribir .xlsx (requerido por pandas)
xlrd>=2.0.0      # Para leer .xls (formato antiguo, opcional)
xlsxwriter>=3.0.0 # Para exportar con formatos avanzados (opcional)

# Otras dependencias comunes (ajusta según tu proyecto)
plotly>=5.15.0   # Visualizaciones interactivas (opcional)
